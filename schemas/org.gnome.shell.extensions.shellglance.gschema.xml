<?xml version="1.0" encoding="UTF-8"?>
<schemalist gettext-domain="shellglance">
  <schema id="org.gnome.shell.extensions.shellglance" path="/org/gnome/shell/extensions/shellglance/">

    <key name="commands" type="s">
      <default>'[]'</default>
      <summary>Commands configuration</summary>
      <description>JSON array of command objects with properties: id, name, command, interval, timeout, enabled</description>
    </key>

    <key name="separator" type="s">
      <default>' | '</default>
      <summary>Separator between outputs</summary>
      <description>String used to separate command outputs in the top bar</description>
    </key>

    <key name="max-length" type="i">
      <default>30</default>
      <summary>Maximum display length</summary>
      <description>Maximum number of characters per command output in the top bar</description>
    </key>

  </schema>
</schemalist>
